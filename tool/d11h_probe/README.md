# D11H Probe

Internal Android/iOS app for NIIMBOT D11H BLE characterization.

The app depends on the root `flutter_niimbot` package through a local path
dependency so protocol research exercises the SDK under development.
