name: niimbot_lib
description: A reliable Flutter SDK for NIIMBOT D11H BLE label printing.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ^3.11.5
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_reactive_ble: 5.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
